server:
  port: 8888
spring:
  application:
    name: selmag-config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/JavaProj/applicationfromgithub/config/cloud/{application}/{profile}